#include <stdio.h>
#include "iccrypto.h"

// 文件 相关接口
SDK_API int File_Encrypt(char *infile, char *outfile) {
    printf("*************** Enter: %s", "File_Encrypt");
    return 1;
}

SDK_API int File_Decrypt(char *infile, char *outfile) {
    printf("*************** Enter: %s", "File_Decrypt");
    return 1;
}